# a basic import
import greeter as g
# use an alias - consider why might we do this?

# sometimes we only want to import what we need

# create a calculator class in the module

# use the new class to return a score to a user

calc = g.Calculator(4,3)

print(calc.sum())

print(id(calc))




